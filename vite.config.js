import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';

export default defineConfig({
    server: {
        host: '0.0.0.0', // Servidor ouve em todas as interfaces
        hmr: {
            host: 'localhost', // Navegador se conecta ao localhost
            clientPort: 5173,  // Porta que o navegador usa
        },
    },
    plugins: [
        laravel({
            input: [
                'resources/scss/app.scss',
                'resources/js/app.js',
            ],
            refresh: true,
        }),
    ],
});